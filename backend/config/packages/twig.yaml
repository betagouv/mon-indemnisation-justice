twig:
  file_name_pattern: '*.twig'
  globals:
    matomo_site_id: '%env(default::MATOMO_SITE_ID)%'
    precontentieux_courriel_equipe: '%env(PRECONTENTIEUX_COURRIEL_EQUIPE)%'

when@test:
  twig:
    strict_variables: true
