{% extends 'base_react.html.twig' %}

{% block title %}Déclarer un bris de porte{% endblock %}

{% block main %}
  {% set user = app.user %}
  <div {{ stimulus_controller('bris_porte_edit',{
    user: user|serialize('json',{groups: 'user:write'}),
    brisPorte: brisPorte|serialize('json',{groups: 'prejudice:read'}),
    userId: user.id,
  }) }}></div>
{% endblock %}
