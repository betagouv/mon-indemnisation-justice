{% extends 'base_react.html.twig' %}

{% block title %}{{'bris_porte.test_eligibilite.title'|trans}}{% endblock %}

{% block body %}
  {% set user = app.user is not null ? app.user : empty_user() %}
  <div {{ stimulus_controller('bris_porte_test_eligibilite',{
    user: user|serialize('json',{groups: 'user:write'}),
    breadcrumb: breadcrumb|serialize('json'),
    version: version|serialize('json')
  }) }}></div>
{% endblock %}
