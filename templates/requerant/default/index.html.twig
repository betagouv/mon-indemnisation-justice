{% extends 'base_react.html.twig' %}

{% block title %}{{'requerant.homepage.title'|trans}}{% endblock %}

{% block body %}
  {% set user = app.user %}
  <div {{ stimulus_controller('dossier_list',{
    user: user|serialize('json',{groups: 'user:read'}),
    brisPortes: brisPortes|serialize('json',{groups: 'sinistre:read'}),
    breadcrumb: breadcrumb|serialize('json'),
    version: {label: 'beta'}
  }) }}></div>
{% endblock %}
